{
  "name": "@getvision/core",
  "version": "0.0.0-6a35e1d-develop",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./server": "./src/server/index.ts",
    "./tracing": "./src/tracing/index.ts",
    "./types": "./src/types/index.ts"
  },
  "scripts": {
    "dev": "tsc --watch",
    "build": "bun build src/index.ts --outdir dist --target bun --minify && bun build:ui",
    "build:ui": "cd ../../apps/web && bun run build && mkdir -p ../../packages/core/dist/ui && cp -r dist/* ../../packages/core/dist/ui/",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "lint": "eslint . --max-warnings 0"
  },
  "license": "MIT",
  "dependencies": {
    "ws": "^8.18.0",
    "nanoid": "^5.0.7",
    "mime-types": "^2.1.35"
  },
  "peerDependencies": {
    "zod": "^3.0.0 || ^4.0.0"
  },
  "peerDependenciesMeta": {
    "zod": {
      "optional": true
    }
  },
  "devDependencies": {
    "zod": "^4.1.11",
    "@repo/eslint-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "@types/mime-types": "^2.1.4",
    "@types/node": "^20.14.9",
    "@types/ws": "^8.5.12",
    "typescript": "5.9.3"
  },
  "files": [
    "dist"
  ],
  "config": {}
}
